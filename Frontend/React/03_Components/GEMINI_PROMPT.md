Create a visual study guide from "DETAILED_NOTES.md" in this folder (03_Components). Requirements:

- Title: "Visual Notes — 03_Components".
- Expand the `Card` component explanation into an annotated example taking `props` and rendering dynamic content.
- Add a small component hierarchy diagram (Mermaid) showing App -> Card (x3).
- Include accessibility checklist for components (alt text, semantic tags, keyboard focus).
- Provide 3 exercises: prop destructuring, PropTypes addition, memoization with `React.memo` and when to use it.
- Output a standalone Markdown file ready for visual presentation.