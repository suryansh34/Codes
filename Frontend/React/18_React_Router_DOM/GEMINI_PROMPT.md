Create a visual study guide from "DETAILED_NOTES.md" in this folder (18_React_Router_DOM). Requirements:

- Title: "Visual Notes — 18_React_Router_DOM".
- Explain router wrapping (BrowserRouter) and show correct placement in `main.jsx` vs `App.jsx` with annotated code.
- Include a routing diagram and best practices for `Link` vs `a` and route nesting.
- Provide tasks: fix wrapping, add a nested route, lazy load a route.
- Output Markdown with diagrams.