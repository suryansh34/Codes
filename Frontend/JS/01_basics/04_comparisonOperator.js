console.log(2>1) // true

// for clean code do not use this methods.
console.log(null > 0);
console.log(null == 0);
console.log(null >= 0); // comparison operator convert null as '0' that why it result true.
console.log(undefined == 0); // always return false in every case.

/* Triple === this checks whether the operator are of same datatype or not then it give output*/
console.log("2" === 2    );


