Create a visual study guide from "DETAILED_NOTES.md" in this folder (21_context_api). Requirements:

- Title: "Visual Notes — 21_context_api".
- Convert the placeholder ThemeContext explanation into an actionable guide: implement `createContext`, `ThemeProvider`, `useContext` examples and illustrate provider placement in `main.jsx`.
- Add diagrams showing provider -> consumer flow and performance tips (splitting contexts).
- Provide exercises: implement ThemeProvider, persist theme to localStorage, refactor Nav to consume context.
- Output Markdown with annotated code and visuals.