Create a visual study guide from "DETAILED_NOTES.md" in this folder (09_functions). Requirements:

- Title: "Visual Notes — 09_functions".
- Expand event handling examples (onWheel, input change) into annotated code blocks and a small flow diagram showing event -> handler -> state change.
- Add performance tips (debounce/throttle) and 3 small exercises (debounce an onWheel handler, log event properties).
- Output Markdown with code samples and visual callouts for performance pitfalls.