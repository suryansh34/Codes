#include<iostream>

int main() {
    // array = a data structure that can hold multiple values 
    // values are accessed by an index number
    // kind of like a variable tahta holds a multiple values

    // array --> elements should be of same datatype.

    // std::string car[] = {"Corvette", "Thrust SSC","BMW X1"};
    // another example of array declaration
    // std::string car[3];

    // car[0] = "Buggatti";
    // car[1] = "Thar";

    // std::cout<< car[0] << '\n';
    // std::cout<< car[2];
    // std::cout<< car[1];

    double prices[] = {5.00, 7.50, 9.99, 15.00};

    std::cout<< prices[3];

    return 0;
}