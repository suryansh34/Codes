Create a visual study guide from "DETAILED_NOTES.md" in this folder (04_Props). Requirements:

- Title: "Visual Notes — 04_Props".
- For the `Card` example: produce an annotated code block that destructures props and shows fallbacks/defaults.
- Add a small diagram showing data flow: Parent (App) -> Card (props).
- Add a "Common mistakes" visual card and a short list of best practices (keys, PropTypes, default props).
- Provide 4 practice tasks including converting to TypeScript types or PropTypes.
- Output: Markdown file with headings, code blocks, and image placeholder suggestions.