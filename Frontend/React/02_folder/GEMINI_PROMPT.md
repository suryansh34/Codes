Create a visual, learner-friendly Markdown study guide from the existing "DETAILED_NOTES.md" content in this folder (02_folder). Requirements:

- Title: "Visual Notes — 02_folder".
- Expand the entry that explains `main.jsx` mounting and `App.jsx` into step-by-step annotated code blocks.
- Include a small diagram (Mermaid) showing the app mount flow: index.html -> main.jsx -> App -> Components.
- Add 2 visual examples (before/after) showing refactor from hard-coded JSX to mapping an array into components.
- Provide 3 practical exercises and one debugging checklist for runtime errors.
- Output one Markdown file, formatted for quick reading and classroom use.